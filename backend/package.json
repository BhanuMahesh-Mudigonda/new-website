{
  "name": "pb-photography-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd ../frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public/",
    "seed": "node seed.js"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "nodemailer": "^9.0.3",
    "openai": "^6.48.0",
    "resend": "^6.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
